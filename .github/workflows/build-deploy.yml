name: Build and deploy code

on: [push, pull_request]

jobs:
    job1:
        runs_on: ubuntu-22.04
        steps:
            - name: Pulling git repo
              uses: actions/checkout@v2
            - name: Say HI to me
              run: echo "Hello, Pratik!"